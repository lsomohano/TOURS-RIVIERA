<section class="reserva-container">

  <h1>Pago de Reservación</h1>
  <p>Tour: <%= tour.nombre %></p>
  <p>Cliente: <%= reserva.nombre_cliente %></p>
  <p>Fecha: <%= reserva.fecha_reserva.toISOString().split('T')[0] %></p>
  <p>Personas: <%= reserva.cantidad_personas %></p>
  <p>Total: $<%= Number(reserva.total_pagado).toFixed(2) %></p>

  <form action="/reservar/pagar/<%= reserva.token_pago %>" method="POST">
    <button type="submit" class="btn btn-success">Pagar ahora</button>
  </form>
</section>